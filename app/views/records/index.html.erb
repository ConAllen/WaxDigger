

<div class="row">
  <%# the ruby code below loops through the record listings properties. and displays it in a
  bootstrap thumb nail / grid system. there should be 4 records accross using "col-md-3"%>
  <% @records.each do |record| %>
    <div class="col-md-3">
      <div class="thumbnail">
        <%= image_tag record.image.url%><br>
          <div class="caption">
            <h3><%= record.Title %><br></h3>
            <%= record.Label %><br>
            <%= record.Format %><br>
            <%= record.Condition %><br>
            <%= record.Selling_Price %><br>
            <%= link_to 'Show', record, class: "btn btn-link" %>
            <%= link_to 'Edit', edit_record_path(record), class: "btn btn-link" %>
            <%= link_to 'Destroy', record, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %>

          </div>
        </div>
      </div>

    <% end %>
</div>



<%= link_to 'New Record', new_record_path, class: "btn btn-link" %> <br>
